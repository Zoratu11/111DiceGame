//This class handles the running of the whole thing. Its just a few lines but I think it makes
//the code easier to read and gives the code better structure
public class Main {
	public static void main(String []args) {
		Game myGame = new Game();
		myGame.startGame();
	}
}
